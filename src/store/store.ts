import { createStore } from 'vuex';
import Messages from './modules/messages';

export default createStore({
  state: {
  },
  modules: {
    messages: Messages,
  },
  mutations: {
  },
  actions: {
  },
  getters: {
  }
});